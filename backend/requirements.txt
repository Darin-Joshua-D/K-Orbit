# MINIMAL REQUIREMENTS - NO RUST COMPILATION NEEDED
# FastAPI Core (Essential)
fastapi==0.70.1  # Legacy stable version
uvicorn==0.15.0  # Compatible with FastAPI 0.70.1
python-multipart==0.0.5
pydantic==1.8.2  # Compatible with all dependencies
python-dotenv==0.19.0  # Compatible version

# Database & Auth (Essential)
supabase==1.0.3  # Older stable version with fewer dependencies
sqlalchemy==1.4.41  # Compatible version

# AI & Vector DB (Essential)
google-generativeai==0.3.2  # Google Gemini API for chat and embeddings

# Document Processing (Essential - minimal set)
PyPDF2==3.0.1
python-docx==1.1.0
openpyxl==3.1.2

# Utilities (Essential)
httpx==0.23.0  # Compatible with all dependencies
PyJWT==2.3.0  # Compatible version
structlog==23.2.0

# Development (Essential)
pytest==7.4.3 